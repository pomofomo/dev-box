---
- name: Install Rust via rustup
  become: true
  become_user: "{{ username }}"
  shell: |
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
  args:
    creates: "{{ ansible_env.HOME if username == 'root' else '/home/' + username }}/.cargo/bin/rustup"
