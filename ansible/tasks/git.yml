---
- name: Install git
  apt:
    name: git
    state: present

- name: Configure git default branch
  become: true
  become_user: "{{ dev_user }}"
  git_config:
    name: init.defaultBranch
    value: main
    scope: global
